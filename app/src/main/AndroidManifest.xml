<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devhouse.comin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Comin">
        <activity android:name=".Auth.JoinInfoActivity"></activity>
        <activity android:name=".Auth.MyInfoActivity" />
        <activity android:name=".Auth.JoinActivity" />
        <activity
            android:name=".Auth.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".Fragment.MarketInfo.MaKetInfoActivity" />
        <activity android:name=".LectureActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>